//
//  UserProvider.swift
//  Assembl
//
//  Created by Kiara Wahnschafft on 4/25/17.
//  Copyright © 2017 KSK. All rights reserved.
//

/*

import Foundation


// For now provides methods to do stuff to the userArray ****
struct UserProvider     {
    enum UserError: String, Error {
        case invalidUser
        case incorrectPassword
        case preexistingUser
    }
    
    func loginUser(name: String, password: String) throws {
    guard let user = usersList.getUser(name: name), user != nil else{
    throw UserError.invalidUser
    }
    if user.getPassword() != password {
    throw UserError.incorrectPassword
    }
    else {
    currentUser = user
    isLoggedIn = true
    // go to other page ****
    }
    
    }
    
    func addUser(user: user) throws {
    guard let user = usersList.getUser(name: name), user == null else{
    throw UserError.preexistingUser
    }
    else {
    // add user to usersList
    }
    
}
 */
